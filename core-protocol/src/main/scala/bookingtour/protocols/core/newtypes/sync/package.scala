package bookingtour.protocols.core.newtypes

/**
  * © Alexey Toroshchin 2019.
  */
package object sync {
  /*@newtype final case class SyncPropertyId(x: Int)
  object SyncPropertyId {
    implicit final val encoderCirce: Encoder[SyncPropertyId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncPropertyId] =
      Decoder.decodeInt.map(_.coerce[SyncPropertyId])
    implicit final val orderCats: Order[SyncPropertyId] =
      (x: SyncPropertyId, y: SyncPropertyId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncSupplierId(x: Int)
  object SyncSupplierId {
    implicit final val encoderCirce: Encoder[SyncSupplierId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncSupplierId] =
      Decoder.decodeInt.map(_.coerce[SyncSupplierId])
    implicit final val orderCats: Order[SyncSupplierId] =
      (x: SyncSupplierId, y: SyncSupplierId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncGroupId(x: Int)
  object SyncGroupId {
    implicit final val encoderCirce: Encoder[SyncGroupId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncGroupId] =
      Decoder.decodeInt.map(_.coerce[SyncGroupId])
    implicit final val orderCats: Order[SyncGroupId] =
      (x: SyncGroupId, y: SyncGroupId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncRoomTypeId(x: Int)
  object SyncRoomTypeId {
    implicit final val encoderCirce: Encoder[SyncRoomTypeId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncRoomTypeId] =
      Decoder.decodeInt.map(_.coerce[SyncRoomTypeId])
    implicit final val orderCats: Order[SyncRoomTypeId] =
      (x: SyncRoomTypeId, y: SyncRoomTypeId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncRoomCategoryId(x: Int)
  object SyncRoomCategoryId {
    implicit final val encoderCirce: Encoder[SyncRoomCategoryId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncRoomCategoryId] =
      Decoder.decodeInt.map(_.coerce[SyncRoomCategoryId])
    implicit final val orderCats: Order[SyncRoomCategoryId] =
      (x: SyncRoomCategoryId, y: SyncRoomCategoryId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncAccommodationId(x: Int)
  object SyncAccommodationId {
    implicit final val encoderCirce: Encoder[SyncAccommodationId] = Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncAccommodationId] =
      Decoder.decodeInt.map(_.coerce[SyncAccommodationId])
    implicit final val orderCats: Order[SyncAccommodationId] =
      (x: SyncAccommodationId, y: SyncAccommodationId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncAccommodationRefId(x: Int)
  object SyncAccommodationRefId {
    implicit final val encoderCirce: Encoder[SyncAccommodationRefId] =
      Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncAccommodationRefId] =
      Decoder.decodeInt.map(_.coerce[SyncAccommodationRefId])
    implicit final val orderCats: Order[SyncAccommodationRefId] =
      (x: SyncAccommodationRefId, y: SyncAccommodationRefId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncBoardingId(x: Int)
  object SyncBoardingId {
    implicit final val encoderCirce: Encoder[SyncBoardingId] =
      Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncBoardingId] =
      Decoder.decodeInt.map(_.coerce[SyncBoardingId])
    implicit final val orderCats: Order[SyncBoardingId] =
      (x: SyncBoardingId, y: SyncBoardingId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncOfferId(x: Int)
  object SyncOfferId {
    implicit final val encoderCirce: Encoder[SyncOfferId] =
      Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncOfferId] =
      Decoder.decodeInt.map(_.coerce[SyncOfferId])
    implicit final val orderCats: Order[SyncOfferId] =
      (x: SyncOfferId, y: SyncOfferId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncOfferRuleId(x: Int)
  object SyncOfferRuleId {
    implicit final val encoderCirce: Encoder[SyncOfferRuleId] =
      Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncOfferRuleId] =
      Decoder.decodeInt.map(_.coerce[SyncOfferRuleId])
    implicit final val orderCats: Order[SyncOfferRuleId] =
      (x: SyncOfferRuleId, y: SyncOfferRuleId) => x.x.compareTo(y.x)
  }

  @newtype final case class SyncCostId(x: Int)
  object SyncCostId {
    implicit final val encoderCirce: Encoder[SyncCostId] =
      Encoder.encodeInt.contramap(_.x)
    implicit final val decoderCirce: Decoder[SyncCostId] =
      Decoder.decodeInt.map(_.coerce[SyncCostId])
    implicit final val orderCats: Order[SyncCostId] =
      (x: SyncCostId, y: SyncCostId) => x.x.compareTo(y.x)
  }*/
}
